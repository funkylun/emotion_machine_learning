认识功能障碍分类，主要完成模型的训练和预测
normal.py和normal_not.py   为程序主入口类，功能是一样的，都是从视频中提取二维情绪特征，一个提取正样本的，一个提取负样本的，可同时运用。
utils.py            为算法辅助工具类
project2_requirements.txt  为环境依赖包，安装好环境即可运用程度主入口类，环境可用命令安装：pip install -e > project2_requirements.txt